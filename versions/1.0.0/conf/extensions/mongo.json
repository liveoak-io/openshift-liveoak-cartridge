{
    module-id:  'io.liveoak.mongo',
    dependencies: ['mongo-launcher'],
    config: {
      db: "liveoak",
      servers: [
          {
            host: '${jboss.bind.address}',
            port: 27017
          }
      ]
    }
}
